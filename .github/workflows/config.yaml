name: Python Mypy Check

on:
  push:
    branches: [type]
  pull_request:
    branches: [type]

jobs:
  mypy-check:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository
      uses: actions/checkout@v4

    - name: Install UV
      uses: astral-sh/setup-uv@v3
      with:
        version: "0.5.0"  # Pin to specific version as recommended

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version-file: "pyproject.toml"

    - name: Install dependencies
      run: |
        uv sync --all-extras --dev
        uv pip install mypy

    - name: Run mypy
      run: uv run mypy . --strict --show-error-codes --pretty